{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/user/Takibi && CHANGED=$(git diff --name-only HEAD 2>/dev/null | head -5 | tr '\\n' ' ') && git add -A && git diff --cached --quiet || (git commit -m \"Auto-deploy: ${CHANGED:-変更を反映}\" && git push -u origin $(git branch --show-current))",
            "timeout": 60,
            "statusMessage": "変更をコミット＆プッシュ中..."
          }
        ]
      }
    ]
  }
}
